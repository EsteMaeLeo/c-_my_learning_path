# c-_my_learning_path
Coursera C++ Programming Fundamentals

For linux terminal run : sudo apt update && sudo apt install build-essential

Compile: g++ filename.cpp -o test_program and run ./test_program

For Windows install: Install MinGW-w64 and compile g++ filename.cpp -o program.exe

Cross-Compile on Linux sudo apt install mingw-w64 compile: x86_64-w64-mingw32-g++ filename.cpp -o program.exe